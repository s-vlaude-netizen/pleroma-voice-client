*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.keystore
*.jks
# Deliberately tracked: releases need one stable signing identity so that an
# update can replace the version before it. See app/build.gradle.
!app/peroma-release.jks
